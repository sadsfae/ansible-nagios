# File Monitor Hosts - inherits all server checks plus file age monitoring
{% from 'monitored_hosts.j2' import render_group with context %}
{{ render_group('filemon_hosts', 'File Monitor Hosts', [], true) }}
